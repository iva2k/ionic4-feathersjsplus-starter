import { ShowHidePassDirective } from './show-hide-pass.directive';

describe('ShowHidePassDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHidePassDirective();
    expect(directive).toBeTruthy();
  });
});
