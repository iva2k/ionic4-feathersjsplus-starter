
// Define TypeScript interface for service `emails`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface EmailBase {
  // !code: interface // !end
}

// tslint:disable-next-line:no-empty-interface
export interface Email extends EmailBase {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
