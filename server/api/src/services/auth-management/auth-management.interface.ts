
// Define TypeScript interface for service `authManagement`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

// tslint:disable-next-line:no-empty-interface
export interface AuthManagementBase {
  // !code: interface // !end
}

// tslint:disable-next-line:no-empty-interface
export interface AuthManagement extends AuthManagementBase {
  // !code: more // !end
}

// !code: funcs // !end
// !code: end // !end
